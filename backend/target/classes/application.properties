spring.application.name=backend

# Use the MySQL Kubernetes service name instead of localhost
spring.datasource.url=jdbc:mysql://mysql:3306/database?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

server.port=8089
server.servlet.context-path=/back
